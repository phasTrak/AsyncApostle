<?xml version="1.0" encoding="utf-8"?>

<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <Authors>phasTrak</Authors>
        <CodeAnalysisRuleSet>$(SolutionDir)Strict.ruleset</CodeAnalysisRuleSet>
        <Company />
        <Copyright>phasTrak</Copyright>
        <Description>Plugin for resharper, for converting code to async.</Description>
        <IsPackable>true</IsPackable>
        <LangVersion>10.0</LangVersion>
        <Nullable>enable</Nullable>
        <PackageId>AsyncApostle.AsyncApostle</PackageId>
        <PackageLicenseUrl>https://opensource.org/licenses/MIT</PackageLicenseUrl>
        <PackageProjectUrl>https://github.com/phasTrak/AsyncApostle</PackageProjectUrl>
        <PackageTags>async</PackageTags>
        <Product />
        <TargetFramework>net472</TargetFramework>
        <Title>AsyncApostle</Title>
        <Version>1.0.0.3-eap07</Version>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
        <DefineConstants>TRACE;DEBUG;NETFRAMEWORK;NET472;RESHARPER</DefineConstants>
        <OutputPath>bin\Resharper\Debug</OutputPath>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
        <DefineConstants>TRACE;RELEASE;NETFRAMEWORK;NET472;RESHARPER</DefineConstants>
        <OutputPath>bin\Resharper\Release</OutputPath>
    </PropertyGroup>

    <ItemGroup>
        <None Remove="AsyncApostle.Rider.nuspec" />
        <None Remove="packages.AsyncApostle.config" />
        <None Remove="packages.AsyncApostle.Rider.config" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="JetBrains.ReSharper.SDK" Version="2021.3.0-eap07" PrivateAssets="All" />
        <PackageReference Include="Wave" Version="[213-eap07]" />
    </ItemGroup>

</Project>